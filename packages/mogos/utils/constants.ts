export enum WeekType {
  Week = 1, // 按周展示
  Date, // 展示最近7天
}

export enum CellStatus {
  Idle = 1,
  Selected,
  Occupied,
  Invalid,
  Expired
}

export enum WorkMode {
  Edit = 1,
  View = 2,
}
